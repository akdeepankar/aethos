import Link from "next/link";
import { ArrowRight, ArrowUpRight, SiteFooter, SiteHeader } from "../_components/site-chrome";
import { ipos } from "../_lib/content";

export default function IposPage() {
  return <><SiteHeader active="IPOs" /><main className="ipo-directory"><section className="page-intro page-intro-dark"><div className="shell"><p className="eyebrow light"><span />IPO intelligence</p><h1>The signal before<br />the <em>listing bell.</em></h1><p>Track current and upcoming Indian IPOs, then read the business—not just the subscription data—where Aethos has published a deep dive.</p></div></section><section className="ipo-list-section"><div className="shell"><div className="ipo-page-status"><span><i className="live-dot" /> Live tracker</span><small>Updated 20 Jul, 09:30 IST · Data from public exchange disclosures</small></div><div className="ipo-list">{ipos.map((ipo) => <article className="ipo-list-row" key={ipo.slug}><div><span className="ipo-sector">{ipo.sector}</span><h2>{ipo.company}</h2></div><div><span>Issue period</span><strong>{ipo.period}</strong></div><div><span>Price band</span><strong>{ipo.price}</strong></div><div><b className={ipo.type === "Mainboard" ? "tag-main" : "tag-sme"}>{ipo.type.toUpperCase()}</b></div><div>{ipo.deepDive ? <Link className="deep-dive-link" href={`/ipos/${ipo.slug}`}>Read deep dive <ArrowUpRight /></Link> : <span className="unavailable">Deep dive unavailable</span>}</div></article>)}</div><p className="ipo-disclaimer">Aethos research is for information and education. It is not investment advice or a recommendation to apply to an IPO.</p></div></section><section className="ipo-page-cta"><div className="shell"><div><p className="eyebrow light"><span />Go beyond the calendar</p><h2>Business first.<br /><em>Offer second.</em></h2></div><Link className="button button-gold" href="/membership">See membership <ArrowRight /></Link></div></section></main><SiteFooter /></>;
}
